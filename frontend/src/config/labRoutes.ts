/**
 * Лабораторные маршруты: анализатор портфеля, бэктест SMA.
 * Отключение: `VITE_ENABLE_LAB_ROUTES=false` в `.env` / сборке.
 */
export const labRoutesEnabled = import.meta.env.VITE_ENABLE_LAB_ROUTES !== 'false'
